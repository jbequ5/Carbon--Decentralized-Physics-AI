apiVersion: v1
kind: Service
metadata:
  name: hydrogen-validator
  labels:
    app: hydrogen-validator
spec:
  selector:
    app: hydrogen-validator
  ports:
    - protocol: TCP
      port: 8091
      targetPort: 8091
  type: ClusterIP
